# 核心依赖
torch>=2.0.0
numpy>=1.24.0

# 向量检索
faiss-cpu>=1.7.4          # GPU 环境改用 faiss-gpu

# 真实嵌入模型（v3 新增）
sentence-transformers>=2.7.0   # 加载 BGE 系列嵌入模型
jieba>=0.42.1                  # 中文分词，用于 BM25 召回

# 图数据库（可选，无 Neo4j 时用 Mock 模式）
# neo4j>=5.0.0

# 数据处理
pandas>=2.0.0
scikit-learn>=1.3.0
langgraph>=0.2.0
langchain-core>=0.3.0
sentence-transformers>=2.7.0
faiss-cpu>=1.7.4
akshare>=1.18.0

# Web 服务（v2.0 新增）
fastapi>=0.110.0
uvicorn>=0.27.0

# 评测与可视化
matplotlib>=3.8.0
plotly>=5.18.0

# 工具
python-dotenv>=1.0.0
tqdm>=4.66.0

# LLM（本地 Ollama，无需额外安装）
# ollama serve && ollama pull qwen2.5:32b
